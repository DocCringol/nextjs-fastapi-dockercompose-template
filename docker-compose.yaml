version: "3.9"
services:
  backend:
    build:
      context: backend
      dockerfile: Dockerfile
    env_file:
      - .env

  frontend:
    build:
      context: frontend
      dockerfile: dev.Dockerfile
    env_file:
      - .env
    ports:
      - ${FRONTEND_PORT}:3000
