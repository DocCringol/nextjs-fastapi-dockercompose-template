<source>
  @type forward
  port 24224
  bind 0.0.0.0
</source>

<match backend.**>
  @type file
  path /fluentd/log/backend
</match>

<match db-api.**>
  @type file
  path /fluentd/log/db-api
</match>

<match frontend.**>
  @type file
  path /fluentd/log/frontend
</match>
